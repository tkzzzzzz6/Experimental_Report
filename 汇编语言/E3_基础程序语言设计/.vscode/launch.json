{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "dosbox",
      "request": "launch",
      "name": "Run ASM",
      "preLaunchTask": "Build ASM",
      "program": "${fileBasenameNoExtension}.exe"
    }
  ]
}